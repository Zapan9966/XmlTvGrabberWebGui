@model TvHeadendCategory

@{
    Layout = "_LayoutCategories.cshtml";
}

    <form method="post" asp-action="DeleteTvHeadendCategory">
        <input asp-for="TvHeadendCategoryId" />
        <input type="hidden" asp-for="Group" />
        <input type="hidden" asp-for="Name" />
        <div class="text-center">
            <h4>Confirmation suppression catégorie TvHeadend</h4>
            <hr />
            <p class="text-center">La catégorie '@Model.Group - @Model.Name' sera définitivement supprimée.<br />Toutes les catégories XML qui lui sont affectées perdrons leur affectation.</p>
            <p>Voulez-vous continuer ?</p>
            <button type="submit" class="btn btn-outline-success mr-5">Oui</button>
            <a class="btn btn-outline-danger ml-5" asp-action="TvHeadendCategories">Non</a>
        </div>
    </form>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {

            $('form').on('submit', function () {
                $(this).find('.btn').prop('disabled', true);
            });

        });
    </script>
}